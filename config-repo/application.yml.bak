# COMMON CONFIGURATION
# Applied to every Config Client unless overridden by the service-specific YAML.

management:
  endpoints:
    web:
      exposure:
        include: health,info

spring:
  jackson:
    serialization:
      write-dates-as-timestamps: false
